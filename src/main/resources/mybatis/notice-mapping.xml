<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.noticeMapper">
	<insert id="noticeWrite" parameterType="noticeDTO">
 		insert into notice values 
 		(notice_num.nextval, #{notice_id}, #{notice_subject}, #{notice_content}, 0, sysdate);
	</insert>
	
	<delete id="noticeDelete" parameterType="int">
		delete notice where notice_num=#{notice_num}
	</delete>
	
	<select id="noticeView" parameterType="int" resultType="noticeDTO">
		select * from notice where notice_num=#{notice_num}
	</select>
	
	<select id="noticeList" parameterType="java.util.Map" resultType="noticeDTO">
		<![CDATA[select notice_num, notice_id, notice_subject, notice_content,
		notice_readcount, to_char(logtime, 'yyyy-mm-dd') as logtime from
		(select rownum rn, tt.* from (select * from notice order by notice_num desc) tt)
		where rn>=#{startNum} and rn<=#{endNum}]]>
	</select>
	
	<select id="getTotal" resultType="int">
		select count(*) as cnt from notice
	</select>
	
	<select id="updateHit" parameterType="int">
		update notice set notice_readcount= notice_readcount + 1 where notice_num=#{notice_num}
	</select>
	
	<select id="noticeModify" parameterType="noticeDTO">
		update notice set notice_subject=#{notice_subject}, notice_content=#{notice_content}
		where notice_num=#{notice_num}
	</select>
</mapper>





